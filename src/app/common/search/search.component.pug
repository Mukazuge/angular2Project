form.d-flex.form-group(#searchForm='ngForm' '(ngSubmit)'='doSearch(search)')
    input.mr-2.form-control('[placeholder]'='searchPlaceHolder', name='search', '[(ngModel)]'='search')
    button.btn.btn-primary('[hidden]'='show') {{buttonText}}
